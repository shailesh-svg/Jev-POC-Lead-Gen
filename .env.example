# Copy to .env and fill in. .env is gitignored and never committed.
# The app also accepts a key pasted into the Settings tab; this file just makes
# it repeatable for development and testing.
TYPESAFE_API_KEY=
